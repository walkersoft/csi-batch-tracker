<Window x:Class="CSI.BatchTracker.Views.ReceivedPurchaseOrderEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CSI.BatchTracker.Views"
        mc:Ignorable="d"
        Title="Purchase Order Editor"
        SizeToContent="WidthAndHeight">
    <StackPanel Orientation="Vertical" Margin="10">

        <Label Content="Purchase Order Header Data" FontWeight="Bold"/>

        <WrapPanel Margin="0,0,0,5">
            <StackPanel Orientation="Vertical">
                <Label Content="PO Number"/>
                <TextBox Width="100"
                         Margin="0,0,5,0"
                         Text="{Binding PONumber, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding UpdatePurchaseOrderCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
            </StackPanel>

            <StackPanel Orientation="Vertical">
                <Label Content="Receiving Date"/>
                <DatePicker Width="110" 
                            VerticalContentAlignment="Center"
                            Margin="0,0,5,0"
                            SelectedDate="{Binding ReceivingDate, UpdateSourceTrigger=PropertyChanged}">
                    <DatePicker.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding UpdatePurchaseOrderCommand}"/>
                    </DatePicker.InputBindings>
                </DatePicker>
            </StackPanel>

            <StackPanel>
                <Label Content="Receiving Operator"/>
                <ComboBox Width="150" 
                          VerticalContentAlignment="Center"
                          ItemsSource="{Binding BatchOperatorsList, UpdateSourceTrigger=PropertyChanged}"
                          DisplayMemberPath="FullName"
                          SelectedIndex="{Binding SelectedOperatorIndex}">
                    <ComboBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding UpdatePurchaseOrderCommand}"/>
                    </ComboBox.InputBindings>
                </ComboBox>
            </StackPanel>

        </WrapPanel>

        <Separator Width="Auto"/>

        <Label Content="Line Item Detail Data" FontWeight="Bold"/>

        <WrapPanel>

            <StackPanel Orientation="Vertical">
                <Label Content="Color"/>
                <ComboBox Width="110" 
                          VerticalContentAlignment="Center"
                          Margin="0,0,5,0"                          
                          ItemsSource="{Binding Colors, UpdateSourceTrigger=PropertyChanged}"
                          SelectedIndex="{Binding SelectedColorIndex}">
                    <ComboBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding UpdateReceivedBatchCommand}"/>
                    </ComboBox.InputBindings>
                </ComboBox>
            </StackPanel>

            <StackPanel Orientation="Vertical">
                <Label Content="Batch Number"/>
                <TextBox Width="120"
                         VerticalContentAlignment="Center"
                         Text="{Binding BatchNumber, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding UpdateReceivedBatchCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
            </StackPanel>

            <StackPanel>
                <Label Content="Quantity"/>
                <TextBox Width="40"
                         VerticalContentAlignment="Center"
                         Text="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Return" Command="{Binding UpdateReceivedBatchCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>
            </StackPanel>

            <Button Width="75" Height="Auto" VerticalAlignment="Bottom" Content="{Binding UpdateText}"
                    Command="{Binding UpdateReceivedBatchCommand}"/>
        </WrapPanel>

        <StackPanel Orientation="Vertical">
            <Label Content="Received Items" FontWeight="Bold"/>
            <DataGrid Width="365"
                      Height="200"
                      Margin="0,0,0,5"
                      HorizontalAlignment="Left" 
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserReorderColumns="False"
                      CanUserSortColumns="False"
                      HeadersVisibility="Column"
                      IsReadOnly="True"
                      ItemsSource="{Binding PurchaseOrderLedger}"
                      SelectedIndex="{Binding ReceivedBatchesSelectedIndex}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Color Name" Width="120" Binding="{Binding ColorName}"/>
                    <DataGridTextColumn Header="Batch Number" Width="120" Binding="{Binding BatchNumber}"/>
                    <DataGridTextColumn Header="Qty. Received" Width="90" Binding="{Binding Quantity}"/>
                </DataGrid.Columns>
            </DataGrid>
            <WrapPanel>
                <Button Width="175" 
                        Margin="0,0,5,0" 
                        Content="Delete Receiving Record"
                        Command="{Binding DeleteReceivingRecordCommand}"/>
                <Button Width="120" 
                        Content="Save Changes"
                        Command="{Binding UpdatePurchaseOrderCommand}"/>
            </WrapPanel>
        </StackPanel>
    </StackPanel>
</Window>